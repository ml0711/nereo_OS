{
  "name": "nereo-os",
  "private": true,
  "version": "0.0.1",
  "description": "nereo OS — Intelligenz- und Bedienschicht über SharePoint-Projektdaten",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "echo \"TODO: dev-orchestrierung (z. B. turbo/concurrently)\" && exit 0",
    "build": "echo \"TODO: build\" && exit 0",
    "lint": "echo \"TODO: lint\" && exit 0",
    "graph:test": "node packages/graph-client/src/test-connection.js",
    "graph:crawl": "node packages/graph-client/src/crawl.js",
    "graph:export": "node packages/graph-client/src/export-index.js",
    "graph:subscribe": "node packages/graph-client/src/subscribe-cli.js",
    "graph:sync": "node packages/graph-client/src/sync-cli.js"
  },
  "engines": {
    "node": ">=22"
  }
}
